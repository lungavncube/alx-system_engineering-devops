Shell, init files, variables and expansions
Create a script that creates an alias.\nCreate a script that prints hello user, where user is the current Linux user.\nAdd /action to the PATH. /action should be the last directory the shell looks into when lAdd /action to the PATH. /action should be the last directory the shell looks into when looking for a program.
